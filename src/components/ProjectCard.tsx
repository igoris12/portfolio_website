import React from 'react'

interface project {
    id: number
    img: string
    title: string
    tools: [string]
    desc: string
    codeSource: string
    ProjectLink: string
}

const ProjectCard = ({}) => {

    return (
        <>
            item
        </>
    )
}

export default ProjectCard